function handleClick(event){
    event.preventDefault()

    const input = document.querySelector('#inputNumber') // Pegando a id inputNumber do HTML
    console.log(input.value) // Pegando o "valor" do formulário input
}